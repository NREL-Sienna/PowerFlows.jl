name: Main - CI

on:
  # `schedule` was previously nested under `push`, which is not a valid trigger key,
  # so the scheduled run never fired. Cadence matches PowerSystems.jl.
  schedule:
    - cron: "0 0 * * 1-5"
  push:
    branches:
      - main
jobs:
  test:
    name: Julia ${{ matrix.julia-version }} - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        julia-version: ['1', 'nightly']
        julia-arch: [x64]
        os: [ubuntu-latest, windows-latest, macOS-latest]

    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        continue-on-error: true
        with:
          version: ${{ matrix.julia-version }}
          arch: ${{ matrix.julia-arch }}
      - uses: julia-actions/julia-buildpkg@v1
        env:
          PYTHON: ""
      - uses: julia-actions/julia-runtest@v1
        continue-on-error: ${{ matrix.julia-version == 'nightly' }}
        env:
          PYTHON: ""
      # Report coverage from a single job. Uploading all six matrix legs sent
      # duplicate reports per commit, including from nightly runs whose tests
      # were allowed to fail.
      - uses: julia-actions/julia-processcoverage@v1
        if: matrix.os == 'ubuntu-latest' && matrix.julia-version == '1'
      # Hard-fail on upload errors: a silent `false` here lets uploads 404 while
      # CI stays green, which is how coverage reporting goes stale unnoticed.
      - uses: codecov/codecov-action@v7
        if: matrix.os == 'ubuntu-latest' && matrix.julia-version == '1'
        with:
          files: ./lcov.info
          disable_search: true
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
          # Required when CODECOV_TOKEN is an organization-level token.
          slug: ${{ github.repository }}
